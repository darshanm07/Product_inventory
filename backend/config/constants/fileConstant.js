const FILE_STATUS = {
    PROCESSING: 0,
    FAILED: 1,
    UPLOADED: 2,
}

const FILE_URI = {
    IMAGE: 'image/',
    VIDEO: 'video/',
    DOCUMENTS: 'documents/',
    RESUMES: 'resumes/',
}

module.exports = {
    FILE_STATUS,
    FILE_URI,
}
